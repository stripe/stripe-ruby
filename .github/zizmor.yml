rules:
  # Actions from stripe/openapi (e.g., stripe/openapi/actions/*) are unpinned on purpose.
  # This is deliberately scoped to that repo so that other actions
  # still require a full commit hash.
  unpinned-uses:
    config:
      policies:
        "stripe/openapi/*": ref-pin
